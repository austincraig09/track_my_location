<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:maps="clr-namespace:Microsoft.Maui.Controls.Maps;assembly=Microsoft.Maui.Controls.Maps"
             x:Class="track_my_location.MainPage"
             Title="Location Debug Example">
    <VerticalStackLayout Padding="20">
        <!-- A Map control to show the user's location. -->
        <maps:Map x:Name="MyMap"
                  HeightRequest="300"
                  VerticalOptions="CenterAndExpand" />
        <!-- A Label to show latitude/longitude or any errors. -->
        <Label x:Name="LocationLabel"
               Text="Waiting for location..."
               HorizontalOptions="Center"
               VerticalOptions="CenterAndExpand"
               FontSize="16" />
        <!-- A button to trigger location fetch (easy for debugging). -->
        <Button Text="Get Current Location"
                Clicked="GetCurrentLocationButton_Clicked"
                HorizontalOptions="Center" />
    </VerticalStackLayout>
</ContentPage>